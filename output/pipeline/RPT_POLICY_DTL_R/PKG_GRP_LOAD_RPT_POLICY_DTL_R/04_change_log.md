# Change Log — PKG_GRP_LOAD_RPT_POLICY_DTL_R

> **Generated by PL/SQL Documenter** | Date: 2026-08-12
> BEFORE: `00_source.sql` | AFTER: `03_standardized.sql`
> Pipeline run: `01_merged.sql` → `02_optimized.sql` → `03_standardized.sql`

---

## Executive Summary

| Metric | BEFORE | AFTER | Delta |
|--------|-------:|------:|------:|
| Lines of code | 2,754 | 2,692 | -62 |
| Internal procedures | 5 (`prc_load_data_dim_gtt`, `PRC_UPD_COL_DETAILS`, `prc_get_cur_data`, `prc_insert_dummy_rec`, `main`) | 4 (`PRC_UPD_COL_DETAILS`, `prc_get_cur_data`, `prc_insert_dummy_rec`, `main`) | -1 |
| Post-load cursors (in `PRC_UPD_COL_DETAILS`) | 18 | 18 | 0 |
| GTTs (temp tables) | 1 (`DIM_PLAN_DESIGN_DIRECTORY_R_GTT`) | 0 | -1 |
| Write passes to `RPT_POLICY_DTL_R` (INSERT/UPDATE) | 20 (1 dummy INSERT + 19 UPDATE) | 20 (1 dummy INSERT + 19 UPDATE) | 0 |
| Write passes to `DIM_PLAN_DESIGN_DIRECTORY_R_GTT` | 1 | 0 | -1 |
| SQL objects referenced | ~19 | ~19 | 0 (2 substituted) |
| Objects renamed/substituted | — | 2 (`FCT_RPT_CROSS_SELL_SUMMARY_R`→`STG_CROSS_SELL_SUMMARY_R`; `DIM_PLAN_DESIGN_DIRECTORY_R_GTT`→direct `DIM_PLAN_DESIGN_DIRECTORY_R`) | 2 |
| Registry recs applied | — | 6 (5 applied, 1 partially/deferred) | 6 |
| Optimizer discoveries applied | — | 10 (MGAP-1, OPP-02,03,04,05,06,07,08,12,14) | 10 |
| Coding standard fixes | — | 9 (STD-01,02,03,04,05,06,09,10,11) | 9 |

**Net effect:** The intra-procedure GTT (`DIM_PLAN_DESIGN_DIRECTORY_R_GTT`) and its dedicated load procedure (`prc_load_data_dim_gtt`) were eliminated, removing one physical write pass and one procedure; 8 dependent cursors now query `DIM_PLAN_DESIGN_DIRECTORY_R` directly. A naming violation was fixed (`FCT_RPT_CROSS_SELL_SUMMARY_R` → `STG_CROSS_SELL_SUMMARY_R`) and one hardcoded 8-branch value mapping was externalized to `REF_LINE_OF_BUSINESS_GROUP_MAP`. The post-load cursor-UPDATE count and total write passes to `RPT_POLICY_DTL_R` are unchanged — the cursor-fold-into-INSERT (M-0058) and full circular-dependency resolution (M-0073/M-0074) were not implemented in code, only the GTT-level portion. 10 optimizer fixes and 9 coding-standard fixes were also applied, including 2 CRITICAL compile-error corrections.

---

## Changes Applied

---

### [M-0028] O. Hybrid Naming Convention Violation — Rename `FCT_RPT_CROSS_SELL_SUMMARY_R` → `STG_CROSS_SELL_SUMMARY_R`

| Field | Value |
|-------|-------|
| Rec ID | `M-0028` |
| Category | `O. Hybrid Naming Convention Violation` |
| Risk | HIGH |
| Applied in stage | `01_merged.sql` |
| Approved | 2026-08-04 (Stage 1 — Merger) |

**What changed:**
`cur_upd_cross_sell_col` in `PRC_UPD_COL_DETAILS` read from `FCT_RPT_CROSS_SELL_SUMMARY_R`, a table whose name combined the `FCT_` and `RPT_` conventions ambiguously. The reference was renamed to `STG_CROSS_SELL_SUMMARY_R` to correctly signal it as an intermediate staging table. Hop-reduction (folding the producer logic into a consumer) was **not** implemented — only the naming fix.

**BEFORE** (`00_source.sql`, lines 490–509):
```sql
	CURSOR cur_upd_cross_sell_col
	IS
	  SELECT frcssr.n_policy_sk_r                           AS n_policy_sk_r
		   ...
		   , frcssr.n_yearmonth_r                           AS n_yearmonth_r
 	    FROM fct_rpt_cross_sell_summary_r frcssr
 	       , rpt_policy_dtl_r rpdr
 	   WHERE rpdr.n_policy_sk_r = frcssr.n_policy_sk_r
 	     AND rpdr.n_yearmonth_r = frcssr.n_yearmonth_r
 	     AND rpdr.n_yearmonth_r = gn_current_month
    GROUP BY frcssr.n_policy_sk_r
 		   ...
```

**AFTER** (`03_standardized.sql`, lines 474–493):
```sql
	CURSOR cur_upd_cross_sell_col
	IS
	  SELECT frcssr.n_policy_sk_r                           AS n_policy_sk_r
		   ...
		   , frcssr.n_yearmonth_r                           AS n_yearmonth_r
 	    FROM stg_cross_sell_summary_r frcssr  -- M-0028: renamed table
 	       , rpt_policy_dtl_r rpdr
 	   WHERE rpdr.n_policy_sk_r = frcssr.n_policy_sk_r
 	     AND rpdr.n_yearmonth_r = frcssr.n_yearmonth_r
 	     AND rpdr.n_yearmonth_r = gn_current_month
    GROUP BY frcssr.n_policy_sk_r
 		   ...
```

**Impact:**
- Renamed reference: `FCT_RPT_CROSS_SELL_SUMMARY_R` → `STG_CROSS_SELL_SUMMARY_R` (1 occurrence in this package).
- GLOBAL scope: physical table rename requires coordination with `PKG_GRP_LOAD_FCT_RPT_CROSS_SELL_SUMMARY_R` (cross-impact pending per decisions.md).
- No row-count or logic change — same source data, different identifier.

---

### [M-0031 + M-0058 + M-0073 + M-0074 (compound)] O. GTT Intra-Procedure Staging / P. Post-Load Cursor UPDATE / N. Circular Update Chain — GTT elimination

| Field | Value |
|-------|-------|
| Rec ID | `M-0031` (primary), `M-0058`, `M-0073`, `M-0074` (overlap notes — savings credited to M-0031) |
| Category | `O. GTT Intra-Procedure Staging — Evaluate CTE Replacement` |
| Risk | LOW (M-0031) / MEDIUM (M-0058) / HIGH (M-0073, M-0074) |
| Applied in stage | `01_merged.sql` |
| Approved | 2026-08-04 (Stage 1 — Merger) |

**What changed:**
The `prc_load_data_dim_gtt` procedure (source lines 215–311, ~97 lines) — which populated the Global Temporary Table `DIM_PLAN_DESIGN_DIRECTORY_R_GTT` from `DIM_PLAN_DESIGN_DIRECTORY_R` — was removed entirely, along with its call and 9.3/9.4 logging block in `PRC_UPD_COL_DETAILS` (source lines 810–851, ~41 lines). The 8 cursors that previously joined the GTT (`cur_upd_theft_ind_col`, `cur_upd_theft_dt_col`, `cur_upd_prs_strs_ind_r`, `cur_upd_elimperiod_col`, `cur_upd_eap_desc_col`, `cur_upd_bereave_desc_col`, `cur_upd_eap_eff_date_col`, `cur_upd_bereavedate_col`) were rewritten to query `DIM_PLAN_DESIGN_DIRECTORY_R` directly, with the `EXISTS(RPT_POLICY_DTL_R ...)` filter restored to preserve the original row-scoping. Note: the cursor structure itself (BULK COLLECT/FORALL) was **not** folded into the main INSERT (M-0058 not implemented), and the cross-cycle circular-read pattern flagged by M-0073/M-0074 was only addressed at the GTT-removal level, not independently re-verified.

**BEFORE** (`00_source.sql`, lines 216–260, `prc_load_data_dim_gtt` — now removed):
```sql
--Procedure prc_load_data_dim_gtt loads coverage codes data into Global Temporary table
PROCEDURE prc_load_data_dim_gtt
IS
    ln_rec_cnt          PLS_INTEGER	                                    := 0 ;
BEGIN
    gv_trcmsg:='9.3.1 Entered into prc_load_data_dim_gtt';
    ...
    INSERT /*+APPEND PARALLEL(TGT,4)*/
      INTO DIM_PLAN_DESIGN_DIRECTORY_R_GTT TGT
        SELECT T4804933.N_PLAN_DESIGN_SK_R
             , T4804933.V_COVERAGE_CODE_R
             , T4804933.N_POLICY_SK_R
          FROM DIM_PLAN_DESIGN_DIRECTORY_R T4804933
         WHERE T4804933.v_active_status_r = 'Y'
           AND T4804933.v_coverage_code_r IN ('IDTHEFT','IDTHEFTEFFDATE','PSINDICATOR','ELIMPERIOD','EAIND', 'EAP'
    			                             ,'BEREAVE','EAP_EFF_DATE','BEREAVEDATE','BENEPERIOD', 'BENEFITDURATION')
    		AND EXISTS (SELECT 1
                          FROM DIM_GRP_POLICY_DIR_R T4817886
                         WHERE T4817886.N_POLICY_SK_R = T4804933.N_POLICY_SK_R
                           AND T4804933.N_PLAN_DESIGN_SRC_VERSION_NO_R = T4817886.N_POLICY_VERSION_NUMBER_R
                           AND T4817886.v_active_status_r = 'Y'
                           AND EXISTS (SELECT 1
                                         FROM RPT_POLICY_DTL_R RPT
                                        WHERE RPT.N_POLICY_SK_R = T4817886.N_POLICY_SK_R
                                          AND RPT.N_YEARMONTH_R = gn_current_month
                                    )
    				    );
    -- ... exception handling omitted ...
END prc_load_data_dim_gtt;
```

**BEFORE** (`00_source.sql`, lines 374–390, `cur_upd_theft_ind_col` — GTT-based):
```sql
--Cursor cur_upd_theft_ind_col to fetch V_ID_THEFT_IND_R
	CURSOR cur_upd_theft_ind_col
	IS
	SELECT t4804933.n_policy_sk_r                              AS n_policy_sk_r
		 , NVL(MAX( t4805277.v_override_description_r),'No')   AS v_id_theft_ind_r
	  FROM fct_plan_design_summary_r t4805277
		 , dim_plan_design_directory_r_gtt t4804933
	 WHERE t4804933.n_plan_design_sk_r = t4805277.n_plan_design_sk_r
	   AND t4804933.v_coverage_code_r = 'IDTHEFT'
  GROUP BY t4804933.n_policy_sk_r;
```

**AFTER** (`03_standardized.sql`, lines 288–306, `cur_upd_theft_ind_col` — direct query, GTT eliminated):
```sql
--Cursor cur_upd_theft_ind_col to fetch V_ID_THEFT_IND_R
	CURSOR cur_upd_theft_ind_col
	IS
	SELECT t4804933.n_policy_sk_r                              AS n_policy_sk_r
		 , NVL(MAX( t4805277.v_override_description_r),'No')   AS v_id_theft_ind_r
	  FROM fct_plan_design_summary_r t4805277
		 , DIM_PLAN_DESIGN_DIRECTORY_R t4804933  -- M-0031: GTT eliminated
	 WHERE t4804933.n_plan_design_sk_r = t4805277.n_plan_design_sk_r
	   AND t4804933.v_coverage_code_r = 'IDTHEFT'
   AND t4804933.v_active_status_r = 'Y'
   AND t4804933.N_PLAN_DESIGN_SRC_VERSION_NO_R = (
           SELECT T4817886.N_POLICY_VERSION_NUMBER_R
             FROM DIM_GRP_POLICY_DIR_R T4817886
            WHERE T4817886.N_POLICY_SK_R = t4804933.N_POLICY_SK_R
              AND T4817886.v_active_status_r = 'Y'
              AND EXISTS (SELECT 1 FROM RPT_POLICY_DTL_R RPT
                           WHERE RPT.N_POLICY_SK_R = T4817886.N_POLICY_SK_R
                             AND RPT.N_YEARMONTH_R = gn_current_month))
  GROUP BY t4804933.n_policy_sk_r;
```

**Impact:**
- Eliminated: `DIM_PLAN_DESIGN_DIRECTORY_R_GTT` global temporary table and its 1 write pass per run.
- Eliminated: `prc_load_data_dim_gtt` procedure (~97 lines) and its call/logging block in `PRC_UPD_COL_DETAILS` (~41 lines) — net ~138 lines removed at the Merger stage (partially offset by other additions, e.g. M-0053 lookup join, standardizer header/comments).
- 8 cursors rewritten to add back an `EXISTS(RPT_POLICY_DTL_R)` filter that had been implicit via the GTT's pre-filtered population — logic-equivalent, not a behavior change.
- Cursor count unchanged (18 before and after) — M-0058 (fold into INSERT) not implemented in this run.
- ⚠️ M-0073/M-0074 circular-dependency risk (cross-cycle read of `DIM_PLAN_DESIGN_DIRECTORY_R`-adjacent data derived from prior-cycle `RPT_POLICY_DTL_R`) is **not confirmed resolved** — only the GTT hop was removed; registry comments still call for further DIM/FCT column-mapping analysis.
- DBA action pending: `DROP TABLE DIM_PLAN_DESIGN_DIRECTORY_R_GTT` — confirm zero other consumers first.

---

### [M-0053] Q. Hardcoded Value Mapping — Externalize `v_line_of_business_group_r` to lookup table

| Field | Value |
|-------|-------|
| Rec ID | `M-0053` |
| Category | `Q. Hardcoded Value Mapping — Externalize to Lookup Table` |
| Risk | LOW |
| Applied in stage | `01_merged.sql` |
| Approved | 2026-08-04 (Stage 1 — Merger) |

**What changed:**
The 8-branch `CASE` expression mapping `v_policy_prefix_r` to `v_line_of_business_group_r` in `prc_get_cur_data`'s main SELECT was replaced with a `LEFT JOIN` to a new table `REF_LINE_OF_BUSINESS_GROUP_MAP` (DDL: `REF_LINE_OF_BUSINESS_GROUP_MAP_DDL.sql`). The companion mapping `v_policy_case_size_r` (9 branches) was **deferred** — its first branch depends on a `v_smartchoice_ind_r = 'Y'` flag override rather than a pure numeric range, requiring analyst sign-off on the REF-table design.

**BEFORE** (`00_source.sql`, lines 1811–1820):
```sql
     , CASE
	       WHEN v_policy_prefix_r IN ('STD','TDB','TDI','DBL', 'VPS', 'ASW', 'MAL', 'CTL', 'ORL', 'COL')
		   THEN 'Weekly Income'
           WHEN dim_grp_policy_dir_r.v_policy_prefix_r IN ('VLT', 'LTD', 'VPL', 'VIP', 'ASL', 'FML', 'MSF')
		   THEN 'LTD'
           WHEN dim_grp_policy_dir_r.v_policy_prefix_r IN ('VG')
		   THEN 'VGTL'
           WHEN dim_grp_policy_dir_r.v_policy_prefix_r IN ('GL', 'GGL', 'SPG')
		   THEN 'Group Life'
           WHEN dim_grp_policy_dir_r.v_policy_prefix_r IN ('SR', 'VAR', 'VAI', 'VCI', 'VHI')
		   THEN 'Personal Accident'
           WHEN dim_grp_policy_dir_r.v_policy_prefix_r IN ('DEN', 'VIS')
		   THEN 'Dental/Vision'
           WHEN dim_grp_policy_dir_r.v_policy_prefix_r IN ('BCD', 'BCL', 'BCM', 'BCS', 'BSC')
		   THEN 'Basic Care'
	    END                                                                                  AS v_line_of_business_group_r
```

**AFTER** (`03_standardized.sql`, line 1761 — SELECT list; join added at `prc_get_cur_data` FROM clause):
```sql
     , lob_map.TARGET_LABEL                                                              AS v_line_of_business_group_r  -- M-0053: CASE replaced with REF_LINE_OF_BUSINESS_GROUP_MAP lookup
...
 LEFT JOIN REF_LINE_OF_BUSINESS_GROUP_MAP lob_map
        ON lob_map.SOURCE_CODE = dim_grp_policy_dir_r.v_policy_prefix_r
```

**Impact:**
- Eliminated: 8-branch hardcoded `CASE` expression; business-code changes now require a data INSERT into `REF_LINE_OF_BUSINESS_GROUP_MAP` instead of a package recompile.
- New object: `REF_LINE_OF_BUSINESS_GROUP_MAP` (DDL + 30 seed rows in `REF_LINE_OF_BUSINESS_GROUP_MAP_DDL.sql`) — DBA must create and populate this table before deploying.
- ⚠️ Deferred: `v_policy_case_size_r` (9-branch CASE) — analyst review pending, not converted.

---

## Changes NOT Applied (Deferred / Noted Only)

| Rec ID | Category | Reason not applied |
|--------|----------|--------------------|
| M-0053 (branch b: `v_policy_case_size_r`) | Q. Hardcoded Value Mapping | First WHEN branch uses a flag override (`v_smartchoice_ind_r = 'Y'`), not a pure range — REF-table design requires analyst sign-off (comment retained in SQL) |
| M-0058 (cursor fold into INSERT) | P. Post-Load Cursor UPDATE | Only the GTT-source rewrite (shared with M-0031) was implemented; the 18 cursors were not folded into the main INSERT SELECT — "needs careful analysis and testing" per decisions.md |
| M-0073 / M-0074 (full circular-dependency resolution) | N. Circular Update Chain | GTT hop removed, but underlying DIM/FCT column-mapping re-analysis explicitly flagged as outstanding by registry comments |
| OPP-01 (cursor consolidation) | Optimizer | Conditionally Safe — pending Optimizer-Cursor sub-agent |
| OPP-09 (current-month filter) | Optimizer | Needs analysis |
| OPP-10 (`FCT_GRP_TRANSACTIONS_R` double-scan) | Optimizer | Unsafe — needs execution plan evidence |
| OPP-15/16/17 (DBA index candidates) | Optimizer | Flagged for DBA, no code change |
| STD-07 (carrier NVL function extraction) | Standardizer | Developer deferred; low deployment risk |
| STD-08 (`SELECT *` in 2 inline views) | Standardizer | Developer deferred; high change risk |

---

## Untracked Changes ⚠️

| # | Change detected | Location (approx.) | Notes |
|---|-----------------|---------------------|-------|
| 1 | MGAP-1: broken `v_policy_case_size_r` CASE / dropped `v_bill_option_r` alias, introduced by the Merger while inserting the M-0053b deferral comment | `01_merged.sql` ~lines 1780–1801 (source ref: 1846–1867) | CRITICAL compile-defect fix by Optimizer stage — corrected before proceeding; not a registry rec |
| 2 | OPP-02: `FCT_GRP_BILLING_POLICY_DTL_R` scalar subquery → `ROW_NUMBER()` | `prc_get_cur_data`, ~line 2062 | Optimizer discovery (Mode A), not in registry |
| 3 | OPP-03: non-sargable `TO_CHAR(D_CYCLE_DATE_R)` filter → sargable range predicate | `prc_get_cur_data` | Optimizer discovery |
| 4 | OPP-04: redundant `EXISTS(RPT_POLICY_DTL_R)` removed from `cur_upd_inforceindicator_cols` | `PRC_UPD_COL_DETAILS`, ~line 719 | Optimizer discovery |
| 5 | OPP-05: `SELECT DISTINCT` removed from `POLICY_DIR_EFF_DATE` inline view | `prc_get_cur_data` | Optimizer discovery |
| 6 | OPP-06: `WHEN OTHERS THEN NULL` → log + non-fatal continue in theft-date parse block | `PRC_UPD_COL_DETAILS` | Optimizer discovery |
| 7 | OPP-07: `SELECT TO_DATE FROM DUAL` in loop → direct PL/SQL assignment | `PRC_UPD_COL_DETAILS`, ~line 994 | Optimizer discovery |
| 8 | OPP-08: added missing `CLOSE cur_upd_client_dtls` | `PRC_UPD_COL_DETAILS`, ~line 768 | Optimizer discovery — resource-leak fix |
| 9 | OPP-12: removed `ORDER BY` from `ft2` inline view in `cur_upd_nxtrenewaleffdt_col` | `PRC_UPD_COL_DETAILS`, ~line 670 | Optimizer discovery |
| 10 | OPP-14: `LIMIT 10000` → `LIMIT gn_bulk_coll_cnt` in `cur_upd_agencycode_cols` | `PRC_UPD_COL_DETAILS`, ~line 1507 | Optimizer discovery; semicolon patched by Planner |
| 11 | STD-01–STD-06, STD-09–STD-11 (9 fixes) | Various — see `decisions.md` Stage 3 section | Standardizer coding-standard fixes, including 2 CRITICAL compile-error corrections (duplicate `EXCEPTION` keyword; wrong logger constant) |

All items above are accounted for in `decisions.md` (Stage 2 Optimizer Decisions, Stage 3 Standardizer Decisions) — flagged here as "untracked" only in the sense that they are not registry `M-####` recs.

---

## Rollback

To revert ALL changes: replace the deployed file with `00_source.sql` (never modified,
always preserved at `output/pipeline/RPT_POLICY_DTL_R/PKG_GRP_LOAD_RPT_POLICY_DTL_R/00_source.sql`).

To revert selectively: refer to each stage file in reverse order:
1. `03_standardized.sql` → undo coding standard fixes only (STD-01…STD-11)
2. `02_optimized.sql`    → undo Optimizer discoveries only (MGAP-1, OPP-02…OPP-14)
3. `01_merged.sql`       → undo all registry rec changes (M-0028, M-0031+M-0058+M-0073+M-0074 compound, M-0053a)

Additional rollback note: if `REF_LINE_OF_BUSINESS_GROUP_MAP` was deployed (M-0053a), revert the `LEFT JOIN` back to the inline `CASE` expression shown in this log's "BEFORE" block for M-0053, or drop/retain the lookup table per DBA discretion.

For cross-package changes (global scope recs): check `decisions.md` for
`00_cross_impact_M-0028.sql`-style files in `PKG_GRP_LOAD_FCT_RPT_CROSS_SELL_SUMMARY_R`'s folder (cross-impact decision still pending per decisions.md).

---

*This change log was auto-generated by the RSL EDP PL/SQL Documenter.
Source: `output/pipeline/RPT_POLICY_DTL_R/PKG_GRP_LOAD_RPT_POLICY_DTL_R/`. To regenerate: `@PL/SQL Documenter PKG_GRP_LOAD_RPT_POLICY_DTL_R`*
